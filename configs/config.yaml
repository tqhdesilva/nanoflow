defaults:
  - config_schema
  - training: default
  - flow: condot
  - experiment: moons
  - _self_

device: cpu
save: false

hydra:
  job:
    chdir: false          # keep cwd at project root (required for torchrun multi-rank)
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}
